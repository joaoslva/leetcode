# Question 3070

## Solution

The secret today was 2D prefix sums, since doing something with O(mxn) time didn't work, obviously haha

## How to Run Tests

### TypeScript

```bash
cd typescript
npm install   # only needed the first time
npm test
```

### Java

```bash
cd java
javac -cp ../../templates/junit-platform-console-standalone.jar Solution.java SolutionTest.java
java -jar ../../templates/junit-platform-console-standalone.jar --class-path . --select-class SolutionTest
```
